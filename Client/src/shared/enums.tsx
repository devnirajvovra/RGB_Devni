export const enum CellTypeEnum {
    EMPTY,
    TILE,
    SOURCE,
}

export const enum GameStatusEnum {
    INITIAL,
    RUNNING,
    FINISHED,
}

export const enum SourcePositionEnum {
    TOP,
    RIGHT,
    BOTTOM,
    LEFT,
}
